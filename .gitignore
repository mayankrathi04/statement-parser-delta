.venv/
__pycache__/
*.pyc
out/
build/
dist/
*.egg-info/

.claude/
.codex-plugin/
.pytest_cache/

# Real statements are financial PII. Keep them out of version control; the
# golden corpus is generated locally from whatever sits in samples/.
samples/*.pdf

# Local database and downloaded statements are financial PII.
statements.db
statements.db-wal
statements.db-shm
data/*.db
data/*.db-wal
data/*.db-shm
inbox/

# Frontend
webapp/node_modules/
webapp/dist/
webapp/tsconfig.tsbuildinfo
sparser/web/dist/
# The whole suite ships. The two files that once held real values — a password
# captured from HDFC's live gate and a real account number in a subject fixture —
# now carry fabricated stand-ins of the same shape, so there is nothing left here
# to withhold. What stays ignored below is the golden corpus, which is a verbatim
# dump of real statements and cannot be sanitised the same way.

# The golden corpus is a verbatim dump of real
# statements — masked card numbers, credit limits, merchant rows. The same PII
# as samples/*.pdf, so it never leaves this machine either. Regenerate it with
# `python scripts/make_golden.py`.
tests/golden/

graphify-out/
.graphify_*
.env
*.bak
